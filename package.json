{
  "name": "google-flower",
  "version": "1.0.0",
  "description": "Converts an obsidian vault inside google drive, to a website using flowershow. Then publishes the website to a github repo.",
  "main": "index.js",
  "scripts": {
    "start:dev": "npx nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js"
  },
  "author": "Gateway3B",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.22",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "googleapis": "^118.0.0"
  }
}
